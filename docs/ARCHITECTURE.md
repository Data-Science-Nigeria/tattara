# Architecture (from PRD)
- Frontend: React + TailwindCSS, IndexedDB (offline), WebSocket for validation feedback
- Backend: Python/FastAPI or Node.js (choose implementation), AI Layer (LLM), DHIS2 service, Generic DB service
- Storage: PostgreSQL (app state), MongoDB (files/audio/images)
- Deployment: On‑prem server
